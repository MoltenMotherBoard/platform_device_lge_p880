From 45ee2f8fd9db6f78cbf20071489206e9c32fd882 Mon Sep 17 00:00:00 2001
From: streambinder <davidepucci@hiddenhost.org>
Date: Tue, 17 Jun 2014 17:04:33 +0200
Subject: [PATCH] libbt: Add support for board-specific configuration

---
 vnd_buildcfg.mk | 4 ++++
 1 file changed, 4 insertions(+)

diff --git a/vnd_buildcfg.mk b/vnd_buildcfg.mk
index b65033a..9aa57f0 100644
--- a/vnd_buildcfg.mk
+++ b/vnd_buildcfg.mk
@@ -1,10 +1,14 @@
 intermediates := $(local-intermediates-dir)
 
+ifneq ($(BOARD_BLUEDROID_VENDOR_CONF),)
+SRC := $(BOARD_BLUEDROID_VENDOR_CONF)
+else
 SRC := $(call my-dir)/include/$(addprefix vnd_, $(addsuffix .txt,$(basename $(TARGET_DEVICE))))
 ifeq (,$(wildcard $(SRC)))
 # configuration file does not exist. Use default one
 SRC := $(call my-dir)/include/vnd_generic.txt
 endif
+endif
 GEN := $(intermediates)/vnd_buildcfg.h
 TOOL := $(TOP_DIR)external/bluetooth/bluedroid/tools/gen-buildcfg.sh
 
-- 
1.8.3.2

